<%#this is just like the _photo partial, but without primary images %>
<tr class ="profile_item_row" id="attachment-<%= document.id %>">
  <td><%= link_to document.filename, document.public_filename %></td>
  <td><%= link_to 'Delete', { :controller => 'account/attachments', :action => 'destroy', :id => document }, :remote => true %></td>
</tr>
